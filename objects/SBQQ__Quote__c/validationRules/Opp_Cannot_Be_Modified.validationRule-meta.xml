<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Opp_Cannot_Be_Modified</fullName>
    <active>true</active>
    <description>Prevents editing of the Opportunity field value when it&apos;s already filled in.</description>
    <errorConditionFormula>AND(
   NOT($Permission.DisableValidationRules),
   NOT(ISNEW()),
   ISCHANGED(SBQQ__Opportunity2__c)
)</errorConditionFormula>
    <errorDisplayField>SBQQ__Opportunity2__c</errorDisplayField>
    <errorMessage>Opportunity cannot be edited once added to a Quote.</errorMessage>
</ValidationRule>

<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Require_Partner_field</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    ISBLANK(SBQQ__Partner__c),
    NOT(ISBLANK( Downstream_Reseller__c )),
    NOT(ISPICKVAL(SBQQ__Opportunity2__r.StageName, &apos;CLOSED WON&apos;)),
    NOT(ISCLONE()),
    NOT($Permission.DisableValidationRules)
)</errorConditionFormula>
    <errorDisplayField>SBQQ__Partner__c</errorDisplayField>
    <errorMessage>Partner field is required if Downstream Partner is populated</errorMessage>
</ValidationRule>

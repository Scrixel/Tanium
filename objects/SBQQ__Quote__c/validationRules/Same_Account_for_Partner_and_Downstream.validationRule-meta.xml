<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Same_Account_for_Partner_and_Downstream</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    TEXT(SBQQ__Status__c) != &apos;Approved&apos;,
    NOT(ISBLANK(SBQQ__Partner__c)),
    NOT(ISBLANK(Downstream_Reseller__c)),
    SBQQ__Partner__c = Downstream_Reseller__c,
    NOT(ISPICKVAL(SBQQ__Opportunity2__r.StageName, &apos;CLOSED WON&apos;)),
    NOT(ISCLONE()),
    NOT($Permission.DisableValidationRules)
)</errorConditionFormula>
    <errorDisplayField>Downstream_Reseller__c</errorDisplayField>
    <errorMessage>Partner and Downstream Partner can’t be the same. Please remove or change one of these fields to save the Quote. Reach out to ChannelOps@tanium.com for any questions.</errorMessage>
</ValidationRule>

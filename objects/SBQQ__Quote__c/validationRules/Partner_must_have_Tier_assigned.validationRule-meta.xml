<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Partner_must_have_Tier_assigned</fullName>
    <active>true</active>
    <description>Ensures that Partner Accounts have an assigned Advantage Program Tier before proceeding</description>
    <errorConditionFormula>AND(
NOT(ISBLANK(SBQQ__Partner__c)),
ISCHANGED(SBQQ__Partner__c),
ISPICKVAL(SBQQ__Partner__r.Partner_Level__c, &quot;&quot;),
NOT($Permission.DisableValidationRules)
)</errorConditionFormula>
    <errorDisplayField>SBQQ__Partner__c</errorDisplayField>
    <errorMessage>The selected Partner must have an Advantage Program Tier assigned.</errorMessage>
</ValidationRule>

<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_VP__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Populated by Process Builder POC Approvers update. When Account has a TAM Owner then it will populate with the Opportunity Area VP. When Account does not have a TAM Owner then it will populate with the Account Area VP.</description>
    <label>Account VP</label>
    <referenceTo>User</referenceTo>
    <relationshipName>POC_Requests</relationshipName>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>

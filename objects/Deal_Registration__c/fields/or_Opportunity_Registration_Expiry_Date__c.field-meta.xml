<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>or_Opportunity_Registration_Expiry_Date__c</fullName>
    <formula>IF( NOT(ISNULL(or_Expiry_Date_New_Opp_Reg__c )) &amp;&amp; NOT(ISNULL(or_Expiry_Date_Extension_Opp_Reg__c)),
IF (or_Expiry_Date_Extension_Opp_Reg__c &lt; or_Expiry_Date_New_Opp_Reg__c, or_Expiry_Date_New_Opp_Reg__c, or_Expiry_Date_Extension_Opp_Reg__c
),
IF(NOT(ISNULL(or_Expiry_Date_New_Opp_Reg__c )),or_Expiry_Date_New_Opp_Reg__c ,
IF(NOT(ISNULL(or_Expiry_Date_Extension_Opp_Reg__c)),or_Expiry_Date_Extension_Opp_Reg__c,null)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Expiry Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
